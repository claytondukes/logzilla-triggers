# Slack verification token (for validating incoming requests)
SLACK_VERIFY_TOKEN=your_slack_verification_token

# Ngrok authentication token (if using ngrok for tunneling)
NGROK_AUTHTOKEN=your_ngrok_auth_token
